# Segredos e ambiente local (manter *.env.example versionado)
.env
**/.env
.env.local
.env.*.local
.envrc

# Chaves e certificados
*.pem
*.p12
*.pfx
*.key
id_rsa
id_rsa.pub
id_ed25519
id_ed25519.pub

# Credenciais cloud / mobile (não commitar)
google-application-credentials.json
application_default_credentials.json
**/google-services.json
**/GoogleService-Info.plist
*-service-account*.json

# Pastas de credenciais de SDKs
.aws/
.azure/
.gcloud/

# Cursor IDE — estado local do projecto (transcripts/terminals podem conter dados sensíveis)
.cursor/projects/

# Node / front (openpolvo)
node_modules/
dist/
# Desktop Electron — nunca versionar binários/pastas unpack (ex.: dist-electron-0.1.x, win-unpacked)
openpolvo/dist-electron/
openpolvo/dist-electron-*/
openpolvo/out/
openpolvo/release/
*.log

# Go (openpolvobackend)
*.exe
*.test
/bin/

# Python (openpolvointeligence)
.venv/
__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/

# IDE / OS
.DS_Store
.idea/
.vscode/
Thumbs.db

# Claude Code local overrides (não versionar)
**/.claude/settings.local.json

scripts/
.cursor/
.claude/