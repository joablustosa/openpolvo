/** Porta Vite dedicada ao preview do estúdio (evita conflito com a shell em 5174). */
export const DEV_STUDIO_PREVIEW_PORT = 5175;

export const DEV_STUDIO_WEBVIEW_PARTITION = "persist:dev-studio-vite-preview";
