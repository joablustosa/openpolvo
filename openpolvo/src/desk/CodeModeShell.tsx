import { DevStudioPanel } from "@/components/dev-studio/DevStudioPanel";

/** Code Mode do Desk MVP — preview + editor inline, sem rotas legacy do SitePanel. */
export function CodeModeShell() {
  return <DevStudioPanel variant="desk" />;
}
