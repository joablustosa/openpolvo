# Exemplo: new_app todo

Pedido utilizador:
```
Cria uma app de tarefas com React, Vite e TypeScript.
```

`request_kind` esperado: `new_app`

Workflow: `new_app` com passos até `workspace_opener`.

Golden: `package.json`, `src/main.tsx`, `src/App.tsx` sob pasta do projecto.
